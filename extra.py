#
# if os.path.isdir("storage"):
#     shutil.rmtree("storage")
#     os.mkdir("storage")
# else:
#     os.mkdir("storage")
#
#
# filename = generate_excel(total, avg_to_min, df, target_Currency, summary_overall, margin_per)